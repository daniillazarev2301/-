Для работы программы используем 3 файла:
    1. Мозги бота (*.py)
    2. Конфиг файл (*.json)
    3. Файл с токеном (token.txt) # Его необходимо самим создать.

Также будет необходимо установить библиотеки:
    1. discord.py

Запускаем main.py и наслаждаемся.
